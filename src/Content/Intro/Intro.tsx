import { Box } from "@chakra-ui/react";

const Intro = () => {
  return (
    <Box className="intro-section">
      <video muted autoPlay loop src="../assets/intro-video.mp4"></video>
      hello
    </Box>
  );
};

export default Intro;
